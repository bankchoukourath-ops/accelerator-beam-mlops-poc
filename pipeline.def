Bootstrap: docker
From: python:3.12-slim

%post
    pip install --no-cache-dir pandas pyarrow h5py

%runscript
    python3 -c '
import pandas as pd
df = pd.read_parquet("mesures_faisceau.parquet")
print("Exécution native du pipeline :")
print(df.head(2))
'
